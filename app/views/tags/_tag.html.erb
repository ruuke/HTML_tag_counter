<tr>
  <td><%= tag.title %></td>
  <td><%= tag.count %></td>
  <td><%= link_to 'Show', tag_path(tag) %></td>
  <td><%= link_to 'Edit', edit_tag_path(tag) %></td>
  <td><%= link_to 'Destroy Tag', tag_path(tag),
               method: :delete,
               data: { confirm: 'Are you sure?' } %>
  </td>
</tr>    
  